<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.timmitof.moneytracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:name=".App"
        android:theme="@style/Theme.MoneyTracker">
        <activity
            android:name="com.timmitof.moneytracker.views.activity.homeActivity.HomeActivity"
            android:exported="false" />
        <activity
            android:name="com.timmitof.moneytracker.views.activity.loginActivity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>