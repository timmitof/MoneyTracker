<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MoneyTracker" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/violet100</item>
        <item name="colorPrimaryVariant">@color/violet60</item>
        <item name="colorOnPrimary">@color/light100</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/light40</item>
        <item name="statusBarBackground">@color/dark75</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppBottomSheetDialogTheme"
        parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle"
        parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bottom_sheet_bg</item>
    </style>
</resources>